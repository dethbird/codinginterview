# Fetch Users (loading + error)

Build <UserList/> that fetches from https://jsonplaceholder.typicode.com/users.
Requirements:
- Show "Loading…" while fetching.
- On success: render list of names.
- On error: show an error message.
(For tests, we'll mock fetch.)

