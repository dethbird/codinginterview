# Form with Inline Validation

Build a SignUp form with fields: email, password, confirm.
Acceptance criteria:
- Email must include '@'; password ≥ 8; confirm === password.
- Show inline errors and disable submit until valid.
- On submit, show "Submitted!" (no network).

