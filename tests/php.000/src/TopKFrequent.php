<?php
declare(strict_types=1);

namespace App;

final class TopKFrequent
{
    /**
     * Return the k most frequent integers in $nums, in any order.
     * Use O(n log n) or better.
     * @param list<int> $nums
     * @return list<int>
     */
    public static function find(array $nums, int $k): array
    {
        // TODO: count frequencies then select top k
        throw new \RuntimeException('TODO');
    }
}
