
# Small 05 — Debounced Autosave
Build `<AutosaveInput storageKey />` that saves text to localStorage after 500ms of no typing.

Requirements:
- Controlled input
- Debounce save with cleanup on change/unmount
- Load previous value on mount
